/**
 * lib/onboarding.ts
 *
 * The PROGRAMME constant and day-based helpers that lived here have been
 * removed. Onboarding progress is computed from real task rows in the
 * database — see components/console/InternTasks.tsx and
 * lib/task-actions.ts.
 *
 * This file is retained as an empty module so existing imports do not
 * need to be hunted down individually; TypeScript will flag any reference
 * to the removed exports as a compile error.
 */
